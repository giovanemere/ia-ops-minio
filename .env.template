# IA-Ops MinIO Portal Integrado - Variables de Entorno
# Template de configuración - NO EDITAR DIRECTAMENTE

# MinIO Configuration
MINIO_ROOT_USER=${MINIO_ROOT_USER:-minioadmin}
MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD:-minioadmin123}
MINIO_ENDPOINT=${MINIO_ENDPOINT:-localhost:9898}

# PostgreSQL Database Configuration
POSTGRES_HOST=${POSTGRES_HOST:-localhost}
POSTGRES_PORT=${POSTGRES_PORT:-5432}
POSTGRES_DB=${POSTGRES_DB:-veritas_db}
POSTGRES_USER=${POSTGRES_USER:-veritas_user}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-veritas_password}
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Ports Configuration
DASHBOARD_PORT=${DASHBOARD_PORT:-6540}
MINIO_API_PORT=${MINIO_API_PORT:-9898}
MINIO_CONSOLE_PORT=${MINIO_CONSOLE_PORT:-9899}
REST_API_PORT=${REST_API_PORT:-8848}
DOCS_PORT=${DOCS_PORT:-6541}

# Storage Configuration
MINIO_DATA_DIR=${MINIO_DATA_DIR:-./data}
MINIO_LOGS_DIR=${MINIO_LOGS_DIR:-./logs}

# Backup Configuration
BACKUP_DIR=${BACKUP_DIR:-./backups}
REPOSITORIES_DIR=${REPOSITORIES_DIR:-/home/giovanemere/ia-ops}
BACKUP_BUCKET=${BACKUP_BUCKET:-repositories-backup}
BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-7}
ONEDRIVE_FOLDER=${ONEDRIVE_FOLDER:-IA-Ops-Backups}
ONEDRIVE_ACCESS_TOKEN=${ONEDRIVE_ACCESS_TOKEN:-}

# Docker Hub Configuration
DOCKER_HUB_USER=${DOCKER_HUB_USER:-edissonz8809}
DOCKER_IMAGE_NAME=${DOCKER_IMAGE_NAME:-ia-ops-minio-integrated}
DOCKER_IMAGE_TAG=${DOCKER_IMAGE_TAG:-latest}

# Security Configuration
JWT_SECRET_KEY=${JWT_SECRET_KEY:-your-secret-key-here}
API_KEY=${API_KEY:-your-api-key-here}

# Logging Configuration
LOG_LEVEL=${LOG_LEVEL:-INFO}
LOG_FORMAT=${LOG_FORMAT:-json}

# Environment
ENVIRONMENT=${ENVIRONMENT:-development}
DEBUG=${DEBUG:-false}
